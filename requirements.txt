# Dépendances pour le prototypage (notebook + scripts src/)
# L'Azure Function a son propre requirements.txt (plus léger).

numpy>=1.24
pandas>=2.0
scikit-learn>=1.3          # PCA, cosine_similarity
scipy>=1.10                # matrices creuses (collaborative filtering)
implicit>=0.7              # ALS pour le filtrage collaboratif (feedback implicite)
threadpoolctl>=3.1         # borne BLAS à 1 thread pendant l'entraînement ALS
matplotlib>=3.7            # visualisations d'exploration
jupyter>=1.0
tqdm>=4.65
